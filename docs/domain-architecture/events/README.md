# Event Contracts

Event contract YAML specs for cross-context integration events.

## Contents

Each event has a YAML spec file named `<event-name>.yaml`. See `../DOMAIN-EVENTS.md` for the narrative and producing/consuming contexts.

## Events

- `CandidateFetched` — `candidate-fetched.yaml`
- `CandidateProfiled` — `candidate-profiled.yaml`
- `CandidateScored` — `candidate-scored.yaml`
- `DecisionRecorded` — `decision-recorded.yaml`
- `RecommendationPublished` — `recommendation-published.yaml`

> Specs are stubs pending first use. Add them when the workflow is automated.