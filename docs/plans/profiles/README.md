# Candidate Profiles

Per-candidate profiles, one file per candidate: `<short-name>.md`. Use `../candidate-profile-template.md` as the starting point.

## Index

- (none yet — profiles are written after `scripts/fetch-candidates.sh` runs)