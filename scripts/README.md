# scripts/

Shell scripts for the review workspace.

- `fetch-candidates.sh` — clone/update candidate skill repos into `skills/<short-name>/` (gitignored)
- `build-matrix.sh` — placeholder for future matrix regeneration from profiles
- `staging/` — explains why no staging target exists (this repo has nothing to deploy)